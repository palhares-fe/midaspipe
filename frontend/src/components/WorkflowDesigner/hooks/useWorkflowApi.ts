// Exemplo de esqueleto para centralizar chamadas de API (opcional, pode ser expandido)
export const useWorkflowApi = () => {
  // Implemente funções como fetchWorkflow, addStep, updateStep, deleteStep, etc.
  // Exemplo:
  // const fetchWorkflow = async (id: string) => { ... }
  // return { fetchWorkflow, addStep, updateStep, deleteStep, ... }
  return {};
};