// Exemplo de esqueleto para hook de lazy save/inatividade (opcional, pode ser expandido)
export const useLazySave = () => {
  // Implemente lógica de save automático por inatividade, mudança de aba, etc.
  // Exemplo:
  // useEffect(() => { ... }, [dependências]);
};